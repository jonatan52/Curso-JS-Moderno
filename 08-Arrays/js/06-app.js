const carrito =[];

const producto1 = {
    nombre:"Televisor 32 pulgadas",
    precio:500
}

const producto2 = {
    nombre:"Celular",
    precio:800
}

let resultado;
resultado = [...carrito, producto1]

console.table(carrito);
console.table(resultado);
